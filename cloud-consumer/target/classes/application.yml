spring:
  application:
    name: cloud-consumer

eureka:
  client:
    service-url:
      defaultZone: http://root:123456@localhost:8761/eureka/
  instance:
    prefer-ip-address: true
server:
  port: 7901

